name = "guess-song-game-api"
main = "worker.js"
compatibility_date = "2026-08-20"

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "RoomDO"

[[migrations]]
tag = "v1"
# 免費方案只支援 SQLite 版的 Durable Objects，一定要用 new_sqlite_classes，
# 用 new_classes 會被擋下來說要付費方案。
new_sqlite_classes = ["RoomDO"]
